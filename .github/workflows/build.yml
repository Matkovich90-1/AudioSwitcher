name: Build Cross-Platform Audio Switcher
on: [push]

# Force the runner to use Node 24 for all actions
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'

jobs:
  # --- WINDOWS BUILD ---
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4.2.2
      - uses: actions/setup-python@v6 
        with:
          python-version: '3.11'
      - name: Build EXE
        run: |
          pip install pyinstaller
          # Using --add-binary for nircmd.exe ensures it's treated as an executable resource
          pyinstaller --onefile --noconsole --icon="app_icon.ico" --add-binary "nircmd.exe;." --add-data "app_icon.ico;." audiochanger.py
      - name: Create Installer
        uses: Minionguyjpro/Inno-Setup-Action@v1.2.2
        with:
          path: installer.iss
      - name: Upload Windows
        uses: actions/upload-artifact@v4.6.0
        with:
          name: Audio-Switcher-Windows
          path: dist/AudioSwitcherInstaller.exe

  # --- DEBIAN/LINUX BUILD ---
  build-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4.2.2
      - uses: actions/setup-python@v6 
        with:
          python-version: '3.11'
      - name: Install Tools
        run: |
          sudo apt-get update
          sudo apt-get install -y python3-tk
          pip install pyinstaller
      - name: Build Binary
        run: pyinstaller --onefile --noconsole audiochanger.py
      - name: Upload Linux Binary
        uses: actions/upload-artifact@v4.6.0
        with:
          name: Audio-Switcher-Linux
          path: dist/audiochanger
